# Cambios — 27/08/2026 — Módulo Actuarial: persistencia desde el modal de descarga PDF

Registro de los cambios realizados el día 27/08/2026 sobre la rama `feature/cuadratica/actuarial`.

## Objetivo

Cerrar el ciclo completo de la feature: conectar el guardado de la simulación
(`POST /actuarial/simulaciones/guardar`) al momento en que el usuario ingresa sus
datos de contacto en el modal de descarga del reporte PDF. El lead se crea en
`POST /leads`, se vincula `leadId` a la simulación persistida y se descarga el
reporte en PDF (pattern `pdf-lib`, espejo del informe de lead-magnet).

## Decisión de producto

- Se **mantienen ambos** caminos: "Guardar resultado" (anónimo, sin contacto) y
  "Descargar reporte PDF" (con captura de contacto → Lead vinculado).
- El reporte PDF es **completo** (header de marca, datos del negocio, escenario,
  resultados con rango P5–P95, CTA y footer legal).

## Resumen de tests

| Suite | Antes | Después | Delta |
|---|---|---|---|
| Shared (vitest) | 113 | 113 | 0 |
| Backend (jest) | 32 | 34 | +2* |
| Frontend (vitest) | 36 | 43 | +7 |
| **Total** | **181** | **190** | **+9** |

\* Los +2 de backend corresponden a la ronda anterior (endpoint `/guardar`); esta
ronda solo tocó frontend.

## Cambios

### Frontend — `apps/frontend/src/app/features/actuarial/`

- **`actuarial.service.ts`** — método `registrarLead(datos: LeadRequest)` → `POST /leads`, devuelve `LeadResponse` (`{ id, recibidoEn }`).
- **`modal-exportar-informe/modal-exportar-informe.component.{ts,html,css}`** (nuevo) — modal de captura de contacto espejo de `modal-captura-lead` (lead-magnet):
  - Form reactivo (nombre, empresa, whatsapp, email) validado con `LeadRequestSchema`.
  - Registra el lead y emite `exportado = output<{ lead, leadId }>`.
  - Maneja errores del envelope sin crashear.
- **`servicios/informe-actuarial-pdf.service.ts`** (nuevo) — genera el reporte en PDF con `pdf-lib`:
  - `generarBytes(datos)` (núcleo testeable, sin DOM) y `generarPdf(datos)` (bytes + descarga).
  - Secciones: encabezado de marca, datos del negocio, escenario simulado (tipo B,
    nº de escenarios, confianza), métricas (precio óptimo y rango P5–P95, piso de
    solvencia, prob. de pérdida, equilibrio, ganancia máxima), CTA y pie legal.
  - Nombre de archivo: `informe-actuarial-<empresa>.pdf`.
- **`actuarial.component.ts`** — `modalAbierto` signal + `abrirModal()`/`cerrarModal()`;
  `guardarResultado(leadId?)` acepta id opcional; `exportarInforme(datos)` cierra el
  modal, persiste con el `leadId` y genera el PDF. La falla de persistencia **no**
  bloquea la descarga (aviso suave con `guardado = 'error'`).
- **`actuarial.component.html`** — botón "Descargar reporte PDF" en el panel de
  resultados + `<app-modal-exportar-informe>` condicional.
- **`actuarial.component.css`** — estilos `.btn-descargar-informe`.

### Sin cambios

- Backend, `packages/shared`, `contracts/openapi.yaml`: los endpoints (`/leads`,
  `/actuarial/simulaciones/guardar`) y los schemas ya existían y están documentados.

## Configuración (build)

- `apps/frontend/angular.json` — `anyComponentStyle.maximumWarning` de `4kB` → `6kB`
  (el `maximumError` sigue en `8kB`). El CSS scoped de `actuarial.component.css`
  (form + dashboard de resultados) es el más grande del proyecto (5.12 kB) y
  superaba el warning de 4 kB. Aprobado por el usuario; config compartida.

## Tests agregados (frontend, +7)

1. `modal-exportar-informe.component.spec.ts` (3): formulario incompleto, registro
   exitoso con emisión de `leadId`, falla del envelope.
2. `informe-actuarial-pdf.service.spec.ts` (2): `generarBytes` devuelve PDF válido
   sin tocar el DOM; fallback percentiles → intervalo.
3. `actuarial.service.spec.ts` (+1): `registrarLead` hace POST a `/leads`.
4. `actuarial.component.spec.ts` (+1): `exportarInforme` cierra el modal, persiste
   con `leadId` y genera el PDF (servicio mockeado).

## Documentación actualizada

- `docs/TESTING_ACTUARIAL.md` — inventario al día (190 tests).

---

# Segunda parte de la jornada — Entrega a `main`

Cierre del ciclo de la feature: desde el commit único hasta el mergeo en `main`
(`45389d2`). Automatización con GitHub CLI.

## 1. Historial atómico en la feature branch

- `82faab0` (commit único de 939 líneas) → reescrito con `git reset --soft` en 6
  commits lógicos (`e2d11ad` registrarLead, `b833496` pdf, `f37aa28` modal,
  `17741fe` wiring, `bea8e05` config, `30f7bd8` docs). Árbol idéntico al original.
- División en 3 PRs atómicos mergeados con squash sobre
  `feature/cuadratica/actuarial`:
  - **PR #18 — `feature/cuadratica/informe-pdf`** (`248a4d2`, squash `72a30a7`, 369 líneas).
  - **PR #19 — `feature/cuadratica/captura-contacto`** (`520f1e8`, squash `b2adae6`, 350 líneas).
  - **PR #20 — `feature/cuadratica/exportar-informe-persistir`** (`ecf3204` rebaseado, squash `0d2b21d`, 220 líneas).
- Se respeta el límite de `<500` líneas por PR; la feature quedó consolidada con
  190 tests verdes.

## 2. GitHub CLI

- Instalado `gh` 2.98.0 (winget) en `C:\Program Files\GitHub CLI\gh.exe`,
  autenticado como `@marianof87`. A partir de acá los PRs y merges se automatizan.

## 3. Entrega a `main`

- **PR #21 — `chore: eliminar gitlink suelto en la raíz`** (`2f473bb`): quita la
  entrada `mutualMetricsAngular` de la raíz, un submódulo accidental (mode `160000`,
  sin `.gitmodules`) que GUIA.md ya señalaba. Squash → `main` `0622ca0`, CI verde.
- **PR #22 — feature → main**: inicialmente **CONFLICTING**. `main` tenía avances
  propios en zonas compartidas (`app.config.ts` de Slice 1 y `openapi.yaml` de la
  calculadora financiera). Resolución:
  - Merge de `origin/main` en la feature branch (`bdd0e4e`) + resolución manual.
  - `apps/frontend/src/app/app.config.ts`: conviven `provideServiceWorker(...)` y
    el `provideAppInitializer(() => inject(SesionService).revalidar())` (revalidación
    de sesión de Slice 1).
  - `contracts/openapi.yaml`: se conserva `/leads` (Lead Magnet) y se integran las
    5 rutas `/finanzas/*` con sus schemas. Validado con parser YAML (`js-yaml`).
  - Suite local tras el merge: **219 tests** (shared 136, backend 34, frontend 49).
    El delta +29 viene de `main` (finanzas y auth), no del módulo actuarial.
  - Squash → `main` **`45389d2`**. PR checks (instalar, lint, tests+coverage, build
    backend, build frontend): todos PASS.
- CI por push sobre `main` post-merge: **verde** (run `33134468537`, 5 jobs).
  `test:cov` pasa sin enforce: los thresholds siguen comentados en
  `packages/shared/vitest.config.ts` y el backend no define `coverageThreshold`.

## 4. Limpieza local

- `main` fast-forward a `45389d2`; borradas `feature/cuadratica/actuarial`, los 3 PR
  branches atómicos y `chore/limpiar-gitlink-suelto`. Árbol de trabajo limpio.

## Números de la entrega

| Métrica | Valor |
|---|---|
| PR #22 (feature → main) | 89 archivos, +7.416 / −78 |
| Squash en `main` | `45389d2` |
| CI `main` post-merge | 5 jobs verdes |
| Suite del monorepo | **219 tests** (shared 136, backend 34, frontend 49) |